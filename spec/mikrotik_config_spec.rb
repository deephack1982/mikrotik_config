require 'spec_helper'

describe MikrotikConfig do
  it 'has a version number' do
    expect(MikrotikConfig::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
